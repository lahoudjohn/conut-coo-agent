# Copy this file to `.env` before running `docker compose up --build`.
# These values are read by Docker Compose for the backend container.

# When the backend runs in Docker and OpenClaw runs on the host machine,
# the backend must reach the host gateway through host.docker.internal.
CONUT_OPENCLAW_GATEWAY_URL=http://host.docker.internal:18789

# Optional: if set, the backend /agent/chat proxy can call the OpenClaw gateway
# from inside Docker without reading the host OpenClaw config file.
CONUT_OPENCLAW_GATEWAY_TOKEN=

# Optional: override if you use a different OpenClaw agent id.
CONUT_OPENCLAW_AGENT_ID=main

# Host-side OpenClaw still needs its own environment variables when you run it
# outside Docker:
# OPENAI_API_KEY=sk-...
# CONUT_API_URL=http://127.0.0.1:8000
